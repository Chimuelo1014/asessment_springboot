```mermaid
graph TD
    subgraph "Infrastructure Layer (Adapters)"
        Controller[REST Controllers]
        RepoImpl[JPA Repository Implementation]
        RiskAdapter[Risk Service Adapter]
        RedisAdapter[Redis Cache Adapter]
    end

    subgraph "Application Layer (Use Cases)"
        RegisterUC[Register Credit App]
        EvaluateUC[Evaluate Credit App]
        GetAffiliateUC[Get Affiliate Data]
    end

    subgraph "Domain Layer (Core)"
        Model[Entities: Affiliate, CreditApplication]
        PortsIn[Input Ports Interfaces]
        PortsOut[Output Ports Interfaces]
    end

    Controller -->|Implements| PortsIn
    PortsIn -->|Used By| RegisterUC
    PortsIn -->|Used By| EvaluateUC
    
    RegisterUC -->|Uses| PortsOut
    EvaluateUC -->|Uses| PortsOut
    
    PortsOut <|.. RepoImpl
    PortsOut <|.. RiskAdapter
    PortsOut <|.. RedisAdapter
    
    RepoImpl --> Model
    RegisterUC --> Model
```
